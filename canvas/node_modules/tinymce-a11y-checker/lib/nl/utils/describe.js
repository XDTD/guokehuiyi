"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = describe;

var _formatMessage = _interopRequireDefault(require("../format-message"));

var strings = _interopRequireWildcard(require("./strings"));

const WORD_COUNT = 4;

function describe(elem) {
  if (!elem || !elem.tagName) {
    return null;
  }

  switch (elem.tagName) {
    case "IMG":
      return "Afbeelding met bestandsnaam " + strings.filename(elem.src);

    case "TABLE":
      return "Tabel die begint met " + strings.firstWords(elem.textContent, WORD_COUNT);

    case "A":
      return "Koppeling maken naar tekst die begint met " + strings.firstWords(elem.textContent, WORD_COUNT);

    case "P":
      return "Alinea die begint met " + strings.firstWords(elem.textContent, WORD_COUNT);

    case "TH":
      return "Tabelkop die begint met " + strings.firstWords(elem.textContent, WORD_COUNT);

    case "H1":
    case "H2":
    case "H3":
    case "H4":
    case "H5":
      return "Koptekst die begint met " + strings.firstWords(elem.textContent, WORD_COUNT);

    default:
      return "Element dat begint met " + strings.firstWords(elem.textContent, WORD_COUNT);
  }
}