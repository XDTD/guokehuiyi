"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;

var _react = _interopRequireDefault(require("react"));

var _enzyme = require("enzyme");

var _colorField = _interopRequireDefault(require("../color-field"));

test("it renders", () => {
  const wrapper = (0, _enzyme.mount)( /*#__PURE__*/_react.default.createElement(_colorField.default, {
    label: "color"
  }));
  expect(wrapper.exists()).toBe(true);
});
test("it calls onChange prop with proper values when the picker changes", () => {
  const changeSpy = jest.fn();
  const wrapper = (0, _enzyme.mount)( /*#__PURE__*/_react.default.createElement(_colorField.default, {
    label: "color",
    value: "rgba(100,100,100,0.7)",
    onChange: changeSpy,
    name: "testing"
  })); // The color picker is wrapped by another color picker, so this lets us get it.

  const colorPicker = wrapper.find("ColorPicker").children("ColorPicker"); // In an ideal world, we could use Simulate here, but for some reason it wasn't working :(
  // So instead we're calling the onChange handler on the ColorPicker directly

  colorPicker.props().onChange({
    r: 100,
    g: 100,
    b: 100,
    a: 0.7
  });
  expect(changeSpy).toHaveBeenCalledWith({
    target: {
      name: "testing",
      value: "rgba(100, 100, 100, 0.7)"
    }
  });
});
describe("stringifyRGBA", () => {
  it("handles rgba", () => {
    const rgba = {
      r: 100,
      g: 100,
      b: 100,
      a: 0.7
    };
    expect(_colorField.default.stringifyRGBA(rgba)).toBe("rgba(100, 100, 100, 0.7)");
  });
  it("handles rgb", () => {
    const rgba = {
      r: 100,
      g: 100,
      b: 100,
      a: 1
    };
    expect(_colorField.default.stringifyRGBA(rgba)).toBe("rgb(100, 100, 100)");
  });
});