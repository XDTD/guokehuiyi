"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = describe;

var _formatMessage = _interopRequireDefault(require("../format-message"));

var strings = _interopRequireWildcard(require("./strings"));

const WORD_COUNT = 4;

function describe(elem) {
  if (!elem || !elem.tagName) {
    return null;
  }

  switch (elem.tagName) {
    case "IMG":
      return "Imagem com nome de ficheiro " + strings.filename(elem.src);

    case "TABLE":
      return "Tabela come\xE7ando com " + strings.firstWords(elem.textContent, WORD_COUNT);

    case "A":
      return "Liga\xE7\xE3o com texto a come\xE7ar com " + strings.firstWords(elem.textContent, WORD_COUNT);

    case "P":
      return "Par\xE1grafo come\xE7ando com " + strings.firstWords(elem.textContent, WORD_COUNT);

    case "TH":
      return "Cabe\xE7alho da tabela come\xE7ando com " + strings.firstWords(elem.textContent, WORD_COUNT);

    case "H1":
    case "H2":
    case "H3":
    case "H4":
    case "H5":
      return "T\xEDtulo come\xE7ando com " + strings.firstWords(elem.textContent, WORD_COUNT);

    default:
      return "Elemento a come\xE7ar com " + strings.firstWords(elem.textContent, WORD_COUNT);
  }
}