"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard");

var _immutable = require("immutable");

var _chai = require("chai");

var actions = _interopRequireWildcard(require("../actions"));

var _reducers = _interopRequireDefault(require("../reducers"));

describe('search/reducers', function () {
  var state = (0, _immutable.fromJS)({
    searchText: 'lion',
    isLoading: false,
    entries: [],
    pagination: {
      page: 1,
      total: 10
    }
  });
  describe('searchText', function () {
    it('is set by setSearchText', function () {
      var next = (0, _reducers.default)(state, actions.setSearchText('elephant'));
      (0, _chai.expect)(next.get('searchText')).to.equal('elephant');
    });
  });
  describe('isLoading', function () {
    it('is set by setSearchLoading', function () {
      var next = (0, _reducers.default)(state, actions.setSearchLoading(true));
      (0, _chai.expect)(next.get('isLoading')).to.equal(true);
    });
  });
  describe('entries', function () {
    it('is set by setSearchEntries', function () {
      var next = (0, _reducers.default)(state, actions.setSearchEntries([]));
      (0, _chai.expect)(next.get('entries')).to.deep.equal((0, _immutable.fromJS)([]));
    });
  });
  describe('page', function () {
    it('is set by setSearchPage', function () {
      var next = (0, _reducers.default)(state, actions.setSearchPage(111));
      (0, _chai.expect)(next.get('pagination').get('page')).to.equal(111);
    });
  });
  describe('total', function () {
    it('is set by setSearchTotal', function () {
      var next = (0, _reducers.default)(state, actions.setSearchTotal(9));
      (0, _chai.expect)(next.get('pagination').get('total')).to.equal(9);
    });
  });
});