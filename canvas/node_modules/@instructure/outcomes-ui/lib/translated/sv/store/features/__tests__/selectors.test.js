"use strict";

var _chai = require("chai");

var _immutable = require("immutable");

var _selectors = require("../selectors");

describe('context/selectors', function () {
  var state = (0, _immutable.fromJS)({
    features: ['feature_1', 'feature_2']
  });
  describe('getFeatures', function () {
    it('retrieves the list of features', function () {
      (0, _chai.expect)((0, _selectors.getFeatures)(state)).to.deep.equal(['feature_1', 'feature_2']);
    });
    it('returns [] when empty', function () {
      var newState = state.setIn(['features'], (0, _immutable.List)());
      (0, _chai.expect)((0, _selectors.getFeatures)(newState)).to.deep.equal([]);
    });
    it('returns [] when unset', function () {
      var newState = state.deleteIn(['features']);
      (0, _chai.expect)((0, _selectors.getFeatures)(newState)).to.deep.equal([]);
    });
  });
});