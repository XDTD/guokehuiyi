"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _chai = require("chai");

var _constants = require("../../../constants");

var _reducers = _interopRequireDefault(require("../reducers"));

describe('features/reducers', function () {
  var state = [];

  var reduce = function reduce(state, type, payload) {
    return (0, _reducers.default)(state, {
      type: type,
      payload: payload
    });
  };

  context('features', function () {
    it('updates features list', function () {
      var features = ['feature_1', 'feature_2'];
      var newState = reduce(state, _constants.SET_FEATURES, features);
      (0, _chai.expect)(newState.toJS()).to.deep.equal(['feature_1', 'feature_2']);
    });
  });
});