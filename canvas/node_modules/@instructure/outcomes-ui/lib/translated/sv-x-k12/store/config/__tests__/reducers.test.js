"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _immutable = require("immutable");

var _chai = require("chai");

var _reducers = _interopRequireDefault(require("../reducers"));

describe('config/reducers', function () {
  describe('config', function () {
    it('remains constant', function () {
      var reducer = (0, _reducers.default)({
        host: 'myhost',
        jwt: 'myjwt'
      });
      var newState = reducer((0, _immutable.Map)(), {
        type: 'foo',
        payload: 'bar'
      });
      (0, _chai.expect)(newState.get('host')).to.equal('myhost');
      (0, _chai.expect)(newState.get('jwt')).to.equal('myjwt');
    });
  });
});