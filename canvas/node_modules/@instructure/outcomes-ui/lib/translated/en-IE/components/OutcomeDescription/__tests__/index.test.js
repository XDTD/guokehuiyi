"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _chai = require("chai");

var _react = _interopRequireDefault(require("react"));

var _enzyme = require("enzyme");

var _index = _interopRequireDefault(require("../index"));

var _checkA11y = _interopRequireDefault(require("../../../test/checkA11y"));

describe('OutcomeDescription', function () {
  function makeProps() {
    var props = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    return Object.assign({
      description: 'Hello there',
      truncate: true
    }, props);
  }

  it('renders outcome description', function () {
    var props = makeProps();
    var wrapper = (0, _enzyme.mount)(_react.default.createElement(_index.default, props));
    (0, _chai.expect)(wrapper.text()).to.match(/Hello there/);
  });
  it('sanitizes an outcome description', function () {
    var props = makeProps({
      description: 'Hello <img src="bigimage" />'
    });
    var wrapper = (0, _enzyme.mount)(_react.default.createElement(_index.default, props));
    (0, _chai.expect)(wrapper.html()).not.to.include('bigimage');
    (0, _chai.expect)(wrapper.html()).not.to.include('img');
  });
  it('renders a TruncateText element if the outcome description is long', function () {
    var props = makeProps({
      description: 'a'.repeat(500)
    });
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_index.default, props), {
      disableLifecycleMethods: true
    });
    (0, _chai.expect)(wrapper.find('TruncateText')).to.have.length(1);
  });
  it('meets a11y standards', function () {
    return (0, _checkA11y.default)(_react.default.createElement(_index.default, makeProps()));
  });
});