"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard");

var _chai = require("chai");

var _immutable = require("immutable");

var actions = _interopRequireWildcard(require("../actions"));

var _reducers = _interopRequireDefault(require("../reducers"));

describe('OutcomeTray/reducers', function () {
  var state = (0, _immutable.fromJS)({
    entries: [],
    pagination: {
      page: 1,
      total: null
    }
  });
  describe('list', function () {
    it('is set by setOutcomeList', function () {
      var next = (0, _reducers.default)(state, actions.setOutcomeList(['1', '2']));
      (0, _chai.expect)(next.get('list')).to.deep.equal((0, _immutable.fromJS)(['1', '2']));
    });
  });
  describe('page', function () {
    it('is set by setListPage', function () {
      var next = (0, _reducers.default)(state, actions.setListPage(111));
      (0, _chai.expect)(next.get('pagination').get('page')).to.equal(111);
    });
  });
  describe('total', function () {
    it('is set by setListTotal', function () {
      var next = (0, _reducers.default)(state, actions.setListTotal(9));
      (0, _chai.expect)(next.get('pagination').get('total')).to.equal(9);
    });
  });
});