"use strict";

var _chai = require("chai");

var _immutable = require("immutable");

var _selectors = require("../selectors");

describe('activePicker/selectors', function () {
  var state = (0, _immutable.fromJS)({
    activePicker: 'current_scope'
  });
  describe('getScope', function () {
    it('retrieves the current scope', function () {
      (0, _chai.expect)((0, _selectors.getScope)(state)).to.equal('current_scope');
    });
    it('returns `` when empty', function () {
      var newState = state.setIn(['activePicker'], '');
      (0, _chai.expect)((0, _selectors.getScope)(newState)).to.equal('');
    });
  });
  describe('isOpen', function () {
    it('returns true if the scope matches', function () {
      (0, _chai.expect)((0, _selectors.isOpen)(state, 'current_scope')).to.equal(true);
    });
    it('returns false if the scope doesnt match', function () {
      (0, _chai.expect)((0, _selectors.isOpen)(state, '')).to.equal(false);
    });
  });
});