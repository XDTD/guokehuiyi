"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _chai = require("chai");

var _react = _interopRequireDefault(require("react"));

var _sinon = _interopRequireDefault(require("sinon"));

var _enzyme = require("enzyme");

var _index = _interopRequireDefault(require("../index"));

var _checkA11y = _interopRequireDefault(require("../../../test/checkA11y"));

describe('OutcomeFolderList', function () {
  var outcomes = [{
    id: '1',
    label: 'ABC',
    title: 'Title1'
  }, {
    id: '2',
    label: 'DEF',
    title: 'Title2'
  }, {
    id: '3',
    label: 'GHI',
    title: 'Title3'
  }];

  function makeProps() {
    var props = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    return Object.assign({
      outcomes: outcomes,
      getOutcomeSummary: _sinon.default.spy(),
      setActiveCollection: _sinon.default.spy()
    }, props);
  }

  it('renders an empty div if there are no ids', function () {
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_index.default, makeProps({
      ids: []
    })), {
      disableLifecycleMethods: true
    });
    (0, _chai.expect)(wrapper.find('OutcomeFolder')).not.to.be.present;
  });
  it('renders a folder for each outcome', function () {
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_index.default, makeProps()), {
      disableLifecycleMethods: true
    });
    (0, _chai.expect)(wrapper.find('OutcomeFolder')).to.have.length(3);
  });
  it('passes the right args to each checkbox', function () {
    var props = makeProps();
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_index.default, props), {
      disableLifecycleMethods: true
    });
    var folder = wrapper.find('OutcomeFolder').first();
    (0, _chai.expect)(folder.prop('outcome')).to.deep.equal({
      id: '1',
      label: 'ABC',
      title: 'Title1'
    });
    (0, _chai.expect)(folder.prop('setActiveCollection')).to.equal(props.setActiveCollection);
  });
  it('meets a11y standards', function () {
    return (0, _checkA11y.default)(_react.default.createElement(_index.default, makeProps()));
  });
});