"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _chai = require("chai");

var _constants = require("../../../constants");

var _reducers = _interopRequireDefault(require("../reducers"));

describe('activePicker/reducers', function () {
  var state = 'begin_scope';

  var reduce = function reduce(state, type, payload) {
    return (0, _reducers.default)(state, {
      type: type,
      payload: payload
    });
  };

  it('SET_SCOPE updates the scope', function () {
    var scope = 'current_scope';
    var newState = reduce(state, _constants.SET_SCOPE, scope);
    (0, _chai.expect)(newState).to.deep.equal('current_scope');
  });
});