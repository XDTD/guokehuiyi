"use strict";

var _chai = require("chai");

var _immutable = require("immutable");

var _selectors = require("../selectors");

describe('OutcomeTray/selectors', function () {
  var scope = 'scopeForTest';
  var state = (0, _immutable.fromJS)({
    scopeForTest: {
      config: {
        contextUuid: 'course_100'
      },
      OutcomePicker: {
        tray: {
          list: [],
          pagination: {
            page: 7,
            total: 100
          }
        }
      }
    }
  });
  describe('getOutcomeList', function () {
    it('returns the outcome list', function () {
      (0, _chai.expect)((0, _selectors.getOutcomeList)(state, scope)).to.deep.equal([]);
    });
  });
  describe('getListPage', function () {
    it('returns the current page', function () {
      (0, _chai.expect)((0, _selectors.getListPage)(state, scope)).to.equal(7);
    });
  });
  describe('getListTotal', function () {
    it('returns the current total', function () {
      (0, _chai.expect)((0, _selectors.getListTotal)(state, scope)).to.equal(100);
    });
  });
});