"use strict";

var _chai = require("chai");

var _immutable = require("immutable");

var _selectors = require("../selectors");

describe('context/selectors', function () {
  var state = (0, _immutable.fromJS)({
    scopeForTest: {
      config: {
        foo: 'bar'
      }
    }
  });
  describe('getConfig', function () {
    it('retrieves the correct value', function () {
      (0, _chai.expect)((0, _selectors.getConfig)(state, 'scopeForTest')).to.deep.equal({
        foo: 'bar'
      });
    });
    it('returns an empty object if no config present', function () {
      (0, _chai.expect)((0, _selectors.getConfig)((0, _immutable.Map)(), 'scopeForTest')).to.deep.equal({});
    });
  });
});