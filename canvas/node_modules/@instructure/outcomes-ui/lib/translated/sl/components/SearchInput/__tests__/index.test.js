"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _chai = require("chai");

var _react = _interopRequireDefault(require("react"));

var _sinon = _interopRequireDefault(require("sinon"));

var _enzyme = require("enzyme");

var _uiIcons = require("@instructure/ui-icons");

var _index = _interopRequireDefault(require("../index"));

var _checkA11y = _interopRequireDefault(require("../../../test/checkA11y"));

describe('SearchInput', function () {
  function makeProps() {
    var props = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    return Object.assign({
      onChange: _sinon.default.spy(),
      onClear: _sinon.default.spy(),
      searchText: ''
    }, props);
  }

  it('includes a search icon', function () {
    var wrapper = (0, _enzyme.mount)(_react.default.createElement(_index.default, makeProps()));
    (0, _chai.expect)(wrapper.find(_uiIcons.IconSearchLine)).to.have.length(1);
  });
  it('includes a clear search icon when text is in the search field', function () {
    var wrapper = (0, _enzyme.mount)(_react.default.createElement(_index.default, makeProps({
      searchText: 'x'
    })));
    (0, _chai.expect)(wrapper.find(_uiIcons.IconEndSolid)).to.have.length(1);
  });
  it('includes a clear search button when text is in the search field', function () {
    var props = makeProps({
      searchText: 'x'
    });
    var wrapper = (0, _enzyme.mount)(_react.default.createElement(_index.default, props));
    (0, _chai.expect)(wrapper.find('IconButton')).to.have.length(1);
  });
  it('calls onClear when button is clicked', function () {
    var props = makeProps({
      searchText: 'x'
    });
    var wrapper = (0, _enzyme.mount)(_react.default.createElement(_index.default, props));
    var clear = wrapper.find('IconButton');
    clear.simulate('click');
    (0, _chai.expect)(props.onClear.calledOnce).to.be.true;
  });
  it('meets a11y standards', function () {
    return (0, _checkA11y.default)(_react.default.createElement(_index.default, makeProps()));
  });
});