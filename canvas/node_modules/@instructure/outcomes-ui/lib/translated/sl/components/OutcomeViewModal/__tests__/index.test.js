"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _chai = require("chai");

var _react = _interopRequireDefault(require("react"));

var _sinon = _interopRequireDefault(require("sinon"));

var _enzyme = require("enzyme");

var _index = _interopRequireDefault(require("../index"));

var _reactRedux = require("react-redux");

var _createMockStore = _interopRequireDefault(require("../../../test/createMockStore"));

var _immutable = require("immutable");

describe('OutcomeViewModal', function () {
  function makeProps() {
    var props = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    return Object.assign({
      outcome: {
        label: 'Foo',
        title: 'Bar',
        scoring_method: {
          scoring_tiers: [{
            description: 'it brings a tier to your eye',
            points: 100
          }]
        }
      },
      closeAlignment: _sinon.default.spy(),
      isOpen: true
    }, props);
  }

  var store = (0, _createMockStore.default)((0, _immutable.fromJS)({}));
  it('displays when isOpen true', function () {
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_index.default, makeProps()));
    var modal = wrapper.find('Modal');
    (0, _chai.expect)(modal.prop('open')).to.be.true;
  });
  it('does not display when isOpen false', function () {
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_index.default, Object.assign({}, makeProps(), {
      isOpen: false
    })));
    var modal = wrapper.find('Modal');
    (0, _chai.expect)(modal.prop('open')).to.be.false;
  });
  it('calls closeAlignment on close', function () {
    var props = makeProps();
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_index.default, props));
    var modal = wrapper.find('Modal');
    modal.simulate('close');
    (0, _chai.expect)(props.closeAlignment.calledWith()).to.be.true;
  });
  it('calls closeAlignment on requestClose', function () {
    var props = makeProps();
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_index.default, props));
    var modal = wrapper.find('Modal');
    modal.simulate('dismiss');
    (0, _chai.expect)(props.closeAlignment.calledWith()).to.be.true;
  });
  it('includes a close button in the footer', function () {
    var props = makeProps();
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_index.default, props));
    var closeButton = wrapper.find('ModalFooter Button');
    (0, _chai.expect)(closeButton.length).to.equal(1);
    closeButton.simulate('click');
    (0, _chai.expect)(props.closeAlignment.calledWith()).to.be.true;
  });
  it('uses standard header', function () {
    var props = makeProps();
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_index.default, props));
    var txt = wrapper.find('ModalHeader Heading Text');
    (0, _chai.expect)(txt.render().text()).to.equal('View Outcome');
  });
  it('uses provided header', function () {
    var props = makeProps({
      header: 'Overriding'
    });
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_index.default, props));
    var txt = wrapper.find('ModalHeader Heading Text');
    (0, _chai.expect)(txt.render().text()).to.equal('Overriding');
  });
  it('renders an outcome view when loaded', function () {
    var props = makeProps();
    var wrapper = (0, _enzyme.mount)(_react.default.createElement(_reactRedux.Provider, {
      store: store
    }, _react.default.createElement(_index.default, props)));
    (0, _chai.expect)(wrapper.find('OutcomeView')).to.have.length(1);
  });
});