import { expect } from 'chai';
import React from 'react';
import { mount } from 'enzyme';
import MasteryCounts from '../index';
import checkA11y from '../../../../test/checkA11y';
describe('OutcomeView MasteryCounts', function () {
  var scoringMethod = {
    id: 1,
    description: 'Some Method',
    points_possible: 5,
    mastery_percent: 0.6,
    algorithm: 'highest',
    algorithm_data: {}
  };

  function makeProps() {
    var props = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    return Object.assign({
      scoringMethod: scoringMethod,
      outcomeResult: {
        masteryCount: 10,
        count: 20
      }
    }, props);
  }

  it('includes a progress bar', function () {
    var wrapper = mount(React.createElement(MasteryCounts, makeProps()));
    expect(wrapper.find('ProgressBar')).to.have.length(1);
  });
  it('meets a11y standards', function () {
    return checkA11y(React.createElement(MasteryCounts, makeProps()));
  });
});