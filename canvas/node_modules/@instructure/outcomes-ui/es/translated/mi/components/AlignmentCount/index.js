var _default_mi_number = Intl.NumberFormat("mi", { style: "decimal" }).format;
import React from 'react';
import PropTypes from 'prop-types';
import { AccessibleContent } from '@instructure/ui-a11y-content';
export default function AlignmentCount(_ref) {
  var count = _ref.count;

  if (count != null) {
    var _s, _n, _s2, _n2;

    var title = (_s = count, _n = +_s, _n === 0 ? "Kaore ng\u0101 putanga e hangai ki" : (_default_mi_number(count) + " ng\u0101 putanga kei te h\u0101ngai"));
    return React.createElement(AccessibleContent, {
      alt: title
    }, React.createElement("span", {
      title: title
    }, (_s2 = count, _n2 = +_s2, _n2 === 0 ? "(0)" : ("(" + _default_mi_number(count) + ")") // eslint-disable-next-line react/jsx-closing-tag-location
    )));
  }

  return React.createElement("span", null);
}
AlignmentCount.propTypes = {
  count: PropTypes.number
};
AlignmentCount.defaultProps = {
  count: null
};