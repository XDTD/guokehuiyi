import { expect } from 'chai';
import React from 'react';
import { mount } from 'enzyme';
import AlignmentCount from '../index';
import checkA11y from '../../../test/checkA11y';
describe('AlignmentCount', function () {
  function makeProps() {
    var props = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    return Object.assign({
      count: 100
    }, props);
  }

  it('renders the count', function () {
    var wrapper = mount(React.createElement(AlignmentCount, makeProps()));
    expect(wrapper.text()).to.match(/\(100\)/);
  });
  it('does not render when count is null', function () {
    var props = makeProps({
      count: null
    });
    var wrapper = mount(React.createElement(AlignmentCount, props));
    expect(wrapper.text()).to.equal('');
  });
  it('meets a11y standards', function () {
    return checkA11y(React.createElement(AlignmentCount, makeProps()));
  });
});