var _default_ptBR_number = Intl.NumberFormat("pt-BR", { style: "decimal" }).format;
import React from 'react';
import PropTypes from 'prop-types';
import { AccessibleContent } from '@instructure/ui-a11y-content';
export default function AlignmentCount(_ref) {
  var count = _ref.count;

  if (count != null) {
    var _s, _n, _i, _s2, _n2, _i2;

    var title = (_s = count, _n = +_s, _n === 0 ? "Nenhum resultado alinhado" : (_i = Math.floor(Math.abs(+_s)), 0 <= _i && _i <= 1 ? "Um resultado est\xE1 alinhado" : _default_ptBR_number(count) + " resultados est\xE3o alinhados"));
    return React.createElement(AccessibleContent, {
      alt: title
    }, React.createElement("span", {
      title: title
    }, (_s2 = count, _n2 = +_s2, _n2 === 0 ? "(0)" : (_i2 = Math.floor(Math.abs(+_s2)), 0 <= _i2 && _i2 <= 1 ? "(1)" : "(" + _default_ptBR_number(count) + ")") // eslint-disable-next-line react/jsx-closing-tag-location
    )));
  }

  return React.createElement("span", null);
}
AlignmentCount.propTypes = {
  count: PropTypes.number
};
AlignmentCount.defaultProps = {
  count: null
};