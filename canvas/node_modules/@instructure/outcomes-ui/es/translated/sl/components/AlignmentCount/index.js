var _default_sl_number = Intl.NumberFormat("sl", { style: "decimal" }).format;
import React from 'react';
import PropTypes from 'prop-types';
import { AccessibleContent } from '@instructure/ui-a11y-content';
export default function AlignmentCount(_ref) {
  var count = _ref.count;

  if (count != null) {
    var _s, _n, _v, _i, _s2, _n2, _v2, _i2;

    var title = (_s = count, _n = +_s, _n === 0 ? "Poravnan ni noben u\u010Dni izid." : (_v = (_s + '.').split('.')[1].length, _i = Math.floor(Math.abs(+_s)), _v === 0 && _i % 100 === 1 ? "En u\u010Dni izid je poravnan." : _default_sl_number(count) + " u\u010Dnih izidov je poravnanih."));
    return React.createElement(AccessibleContent, {
      alt: title
    }, React.createElement("span", {
      title: title
    }, (_s2 = count, _n2 = +_s2, _n2 === 0 ? "(0)" : (_v2 = (_s2 + '.').split('.')[1].length, _i2 = Math.floor(Math.abs(+_s2)), _v2 === 0 && _i2 % 100 === 1 ? "(1)" : "(" + _default_sl_number(count) + ")") // eslint-disable-next-line react/jsx-closing-tag-location
    )));
  }

  return React.createElement("span", null);
}
AlignmentCount.propTypes = {
  count: PropTypes.number
};
AlignmentCount.defaultProps = {
  count: null
};