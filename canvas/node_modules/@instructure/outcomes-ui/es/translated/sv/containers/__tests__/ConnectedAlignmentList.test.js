import { expect } from 'chai';
import React from 'react';
import { Provider } from 'react-redux';
import { mount } from 'enzyme';
import { fromJS } from 'immutable';
import createMockStore from '../../test/createMockStore';
import ConnectedAlignmentList from '../ConnectedAlignmentList';
describe('ConnectedAlignmentList', function () {
  it('renders', function () {
    var store = createMockStore(fromJS({
      OutcomePicker: {
        state: 'loading',
        scope: 'scopeForTest'
      }
    }));
    var wrapper = mount(React.createElement(Provider, {
      store: store
    }, React.createElement(ConnectedAlignmentList, {
      scope: "scopeForTest",
      addModal: React.createElement("div", null)
    })));
    expect(wrapper.find('AlignmentList')).to.have.length(1);
  });
});