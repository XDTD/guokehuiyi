var _default_enCY_number = Intl.NumberFormat("en-CY", { style: "decimal" }).format;
import React from 'react';
import PropTypes from 'prop-types';
import { AccessibleContent } from '@instructure/ui-a11y-content';
export default function AlignmentCount(_ref) {
  var count = _ref.count;

  if (count != null) {
    var _s, _n, _i, _v, _s2, _n2, _i2, _v2;

    var title = (_s = count, _n = +_s, _n === 0 ? "No outcomes are aligned" : (_i = Math.floor(Math.abs(+_s)), _v = (_s + '.').split('.')[1].length, _i === 1 && _v === 0 ? "One outcome is aligned" : _default_enCY_number(count) + " outcomes are aligned"));
    return React.createElement(AccessibleContent, {
      alt: title
    }, React.createElement("span", {
      title: title
    }, (_s2 = count, _n2 = +_s2, _n2 === 0 ? "(0)" : (_i2 = Math.floor(Math.abs(+_s2)), _v2 = (_s2 + '.').split('.')[1].length, _i2 === 1 && _v2 === 0 ? "(1)" : "(" + _default_enCY_number(count) + ")") // eslint-disable-next-line react/jsx-closing-tag-location
    )));
  }

  return React.createElement("span", null);
}
AlignmentCount.propTypes = {
  count: PropTypes.number
};
AlignmentCount.defaultProps = {
  count: null
};